# 儿童学习视频管理平台实施任务

> 规格来源：[spec.md](./spec.md)  
> 状态：进行中（已完成项目骨架、数据库、认证、媒体 API 和管理端首版）  
> 目标：将现有纯静态站升级为公开内容站、管理员后台和权限后端。

## 0. 实施原则

- [ ] 所有功能以 `spec.md` 为准；需求变化先更新规格再修改实现。
- [ ] 迁移前备份当前 `videos/`、`posters/` 和 `videos.json`。
- [x] 不在代码、日志、Git 或前端包中保存管理员密码和会话秘密。
- [x] 后端权限校验是唯一授权依据，不能依赖前端隐藏按钮。
- [x] 大文件使用流式上传，禁止将完整视频读入内存。
- [x] Node 校验媒体访问权限，Nginx 使用 `X-Accel-Redirect` 传输视频内容。

## 1. 项目骨架与开发环境

### 1.1 Monorepo

- [x] 创建 npm workspace 根配置。
- [x] 创建 `apps/web` React、Vite、TypeScript 项目。
- [x] 创建 `apps/api` Fastify、TypeScript 项目。
- [x] 创建 `packages/contracts` 共享 Zod schema 和类型。
- [ ] 配置统一 TypeScript、格式化和 lint 规则。
- [x] 配置开发环境前端代理 `/api` 和 `/media`。
- [x] 提供 `.env.example`，不包含真实秘密值。

### 1.2 基础脚本

- [x] 增加开发、构建、测试和类型检查命令。
- [x] 增加数据库迁移命令。
- [x] 增加管理员创建命令。
- [x] 增加现有目录导入命令。
- [ ] 增加备份和回收目录清理命令。

### 1.3 验证

- [x] 全新 checkout 后可以一条命令安装依赖。
- [x] 前端和 API 可以并行启动。
- [x] 生产构建无 TypeScript、lint 或 bundle 错误。

## 2. 数据库与持久化

### 2.1 Schema

- [x] 创建 `users` 表。
- [x] 创建 `sessions` 表。
- [x] 创建 `units` 表。
- [x] 创建 `videos` 表。
- [x] 创建 `audit_logs` 表。
- [x] 为用户名、session token、Unit slug、状态和排序增加必要索引。
- [x] 启用 SQLite foreign keys 和 WAL 模式。

### 2.2 Migration

- [x] 建立可重复执行的 Drizzle migration 流程。
- [x] 创建初始 migration。
- [x] 编写数据库启动检查，禁止在 migration 缺失时静默运行。
- [ ] 为测试创建独立临时数据库。

### 2.3 数据访问

- [ ] 实现 users repository。
- [ ] 实现 sessions repository。
- [ ] 实现 units repository。
- [ ] 实现 videos repository。
- [ ] 实现 audit logs repository。
- [ ] 为关键事务增加失败回滚测试。

## 3. 管理员认证与授权

### 3.1 管理员创建

- [x] 实现交互式 `create-admin` CLI。
- [x] 用户名标准化并校验唯一性。
- [x] 使用 Argon2id 哈希密码。
- [x] CLI 不回显密码，不在命令参数中接受明文密码。

### 3.2 Session

- [x] 实现随机会话令牌生成。
- [x] 数据库只保存令牌哈希。
- [x] 设置 `HttpOnly`、`Secure`、`SameSite=Lax` Cookie。
- [ ] 实现会话过期、刷新、退出和撤销。
- [x] 修改密码后撤销其他会话。

### 3.3 API

- [x] 实现 `POST /api/auth/login`。
- [x] 实现 `POST /api/auth/logout`。
- [x] 实现 `GET /api/auth/me`。
- [x] 实现 `PUT /api/auth/password`。
- [x] 实现管理员鉴权 hook。
- [x] 实现 CSRF 校验。
- [x] 实现登录限速和统一失败消息。
- [x] 记录登录成功、失败、退出和改密日志。

### 3.4 测试

- [ ] 覆盖正确登录、错误密码、禁用用户和过期会话。
- [ ] 覆盖未登录和无权限访问管理 API。
- [ ] 覆盖 CSRF 缺失或错误。
- [ ] 验证响应和日志不泄露密码或 token。

## 4. Unit API

- [x] 定义 Unit Zod 请求和响应 schema。
- [x] 实现公开 catalog 中的 Unit 查询。
- [x] 实现管理端 Unit 列表。
- [x] 实现新建 Unit。
- [x] 实现编辑标题、副标题、slug、排序和状态。
- [ ] 实现批量排序。
- [x] 实现 Unit 发布、下架和归档。
- [x] 阻止删除仍包含有效视频的 Unit。
- [x] 为所有管理写操作记录审计日志。
- [ ] 覆盖 slug 冲突、非法排序和状态转换测试。

## 5. 媒体存储与视频上传

### 5.1 文件目录

- [x] 创建 `data/uploads` 临时目录。
- [x] 创建 `data/media/videos` 正式视频目录。
- [x] 创建 `data/media/posters` 正式封面目录。
- [x] 创建 `data/trash` 回收目录。
- [x] 启动时检查目录存在且可写。
- [x] 将所有运行数据加入 `.gitignore`。

### 5.2 视频上传管线

- [x] 配置 Fastify multipart 流式上传。
- [x] 配置 2 GB 默认大小限制和上传超时。
- [x] 使用 UUID 生成存储名。
- [x] 校验 `.mp4`、真实 MIME 和文件签名。
- [x] 调用 ffprobe 读取时长、分辨率和编码。
- [x] 只接受 H.264 视频和允许的音频编码。
- [x] 通过校验后原子移动到正式目录。
- [x] 数据库写入失败时清理已移动文件。
- [x] 请求失败、连接中断或超时时清理临时文件。

### 5.3 自动封面

- [x] 未上传封面时调用 ffmpeg 生成默认帧。
- [x] 支持短视频时间点回退。
- [x] 输出适合 640×360 卡片的封面。
- [x] 自动封面失败时视频保存为草稿并展示告警。

### 5.4 封面上传

- [x] 接受 JPG、PNG 和 WebP。
- [x] 配置 10 MB 默认大小限制。
- [x] 使用 Sharp 检查真实格式。
- [x] 删除图片元数据并输出 640×360 WebP。
- [x] 实现替换封面和旧封面清理。
- [x] 实现删除自定义封面后重新生成默认封面。

## 6. 视频管理 API

- [ ] 定义视频 Zod 请求和响应 schema。
- [x] 实现管理端视频分页列表和筛选。
- [x] 实现视频详情查询。
- [x] 实现 `POST /api/admin/videos` 上传接口。
- [x] 实现编辑标题、Unit、排序和状态。
- [ ] 实现批量排序。
- [x] 实现发布和下架。
- [x] 实现替换和删除封面。
- [x] 实现视频软删除。
- [x] 实现回收期内恢复。
- [x] 实现 7 天后永久清理任务。
- [x] 为所有操作记录审计日志。

## 7. 公开 API

- [x] 实现 `GET /api/catalog`。
- [x] 只返回已发布 Unit 下的已发布视频。
- [x] 按 `sortOrder` 数字排序。
- [x] 返回 `/media/...` 视频和封面 URL。
- [x] 不返回服务器路径、原始临时路径和管理员字段。
- [x] 实现媒体访问路由：公开内容允许访客访问，草稿和下架内容仅管理员可访问。
- [x] 媒体路由通过 `X-Accel-Redirect` 交给 Nginx 传输文件。
- [x] 实现 `GET /api/units/:slug`。
- [x] 实现 `GET /api/health`。
- [x] 为 catalog 添加合理缓存和 ETag。
- [ ] 覆盖草稿、下架和软删除不可见测试。

## 8. 公开前端迁移

### 8.1 页面迁移

- [x] 将当前 `index.html`、`styles.css` 和 `app.js` 迁移到 React/Vite。
- [x] 保留当前儿童友好视觉和触控体验。
- [x] 将 `videos.json` 数据源替换为 `/api/catalog`。
- [x] 保留 Unit 切换和数字自然排序。
- [x] 保留视频弹层、上一集、下一集和返回列表。
- [x] 保留封面懒加载和占位回退。

### 8.2 播放体验

- [x] 使用后端返回的 `/media/...` URL。
- [x] 验证 MP4 Range 和进度拖动。
- [ ] 处理视频 404、加载失败和网络中断。
- [ ] 手机、平板和桌面端完成响应式测试。
- [ ] 完成键盘、焦点和 Escape 关闭测试。

## 9. 管理后台前端

### 9.1 登录

- [x] 实现 `/admin/login`。
- [x] 实现用户名、密码、提交中和错误状态。
- [x] 登录后跳转 `/admin`。
- [x] 未登录访问管理路由自动跳转。
- [ ] 会话过期时清理本地状态并跳转登录。

### 9.2 后台框架

- [x] 实现管理端导航和退出登录。
- [x] 实现 Unit 数、视频数、草稿数和最近上传摘要。
- [x] 实现统一的成功、错误和确认反馈。
- [x] 管理后台兼容桌面和平板。

### 9.3 Unit 页面

- [x] 实现 Unit 列表。
- [x] 实现新建和编辑表单。
- [ ] 实现拖动或批量排序。
- [x] 实现发布、下架、归档和删除确认。

### 9.4 视频页面

- [ ] 实现视频列表、分页、Unit 和状态筛选。
- [x] 实现视频上传表单。
- [x] 使用 XHR 或等效方式显示上传进度，并支持多个视频任务排队、串行上传。
- [ ] 实现封面选择、裁剪预览和替换。
- [ ] 实现视频详情和媒体信息展示。
- [x] 实现标题、Unit、排序和状态编辑。
- [x] 实现发布、下架、软删除和恢复确认。
- [ ] 上传中离开页面需要确认。

### 9.5 操作记录

- [ ] 实现审计日志列表。
- [ ] 支持按管理员、动作和时间筛选。
- [ ] 不展示敏感字段。

## 10. Nginx 与 Docker 部署

### 10.1 Docker

- [x] 创建多阶段前端构建镜像。
- [x] 创建包含 Node、ffmpeg、ffprobe 和 Sharp 运行环境的 API 镜像。
- [x] 创建 `docker-compose.yml`。
- [x] 为数据库、媒体、上传和回收目录配置持久化卷。
- [x] 配置容器健康检查和自动重启策略。

### 10.2 Nginx

- [x] `/` 和 `/admin/*` 提供前端并支持 SPA fallback。
- [x] `/api/*` 反向代理到 Fastify。
- [x] `/media/*` 先代理到 API 做权限检查。
- [x] 配置仅供 `X-Accel-Redirect` 使用的 Nginx internal 媒体 location。
- [x] 配置 MP4 MIME、HTTP Range 和缓存。
- [x] 配置上传大小和上传超时。
- [ ] 配置 HTTPS、HTTP 跳转和安全响应头。
- [x] 防止浏览媒体目录列表。

### 10.3 配置与文档

- [x] 提供 `.env.example`。
- [x] 编写首次启动和管理员创建说明。
- [ ] 编写升级和 migration 说明。
- [ ] 编写媒体目录权限说明。
- [ ] 编写备份和恢复说明。

### 10.4 CI/CD 镜像发布

- [x] 增加 GitHub Actions 的类型检查、lint 和测试门禁（`main` 推送和版本 tag 均执行）。
- [x] 推送 `v*.*.*` tag 时构建并发布 API、Web 两个 GHCR 镜像。
- [x] 为 Docker Compose 增加可替换的镜像 tag 配置。
- [x] 在 README 中记录 tag 发布和服务器拉取部署流程。

## 11. 当前内容迁移

- [ ] 备份当前 `videos/`、`posters/` 和 `videos.json`。
- [ ] 实现 `import-current-catalog` 脚本。
- [x] 导入 Unit 1 和 Unit 2。
- [x] 导入 24 个视频和 24 个封面。
- [x] 使用 UUID 重命名目标媒体文件。
- [x] 通过 ffprobe 补充媒体信息。
- [x] 默认导入为已发布。
- [x] 导入脚本可重复运行且不会创建重复记录。
- [ ] 输出成功、跳过和失败报告。
- [x] 对比公开 API 和旧 `videos.json` 的标题及排序。
- [ ] 新系统验证完成前不删除旧媒体。

## 12. 安全与可靠性检查

- [ ] 所有管理 API 验证 session、role 和 CSRF。
- [ ] 登录和上传接口启用限速。
- [ ] 所有输入设置类型和长度限制。
- [ ] 上传文件不使用原始文件名作为存储路径。
- [ ] 非法文件、路径穿越和双扩展名测试通过。
- [ ] Cookie 和安全响应头检查通过。
- [ ] 日志不包含密码、Cookie、token 和媒体内容。
- [ ] 上传中断和数据库失败不会留下孤儿文件。
- [ ] SQLite、媒体和回收目录备份成功。
- [ ] 执行一次完整恢复演练。

## 13. 测试计划

### 13.1 单元测试

- [ ] Zod schema 和状态转换。
- [ ] 密码哈希和 session token。
- [ ] Unit 和视频排序。
- [ ] 媒体路径生成和文件类型判断。
- [ ] 自动封面时间点计算。

### 13.2 API 集成测试

- [ ] 认证、退出、过期和权限拒绝。
- [ ] Unit CRUD、排序和状态。
- [ ] 视频上传、编辑、发布、删除和恢复。
- [ ] 封面上传、替换和自动生成。
- [ ] 上传失败清理和事务补偿。
- [ ] 公开 catalog 内容过滤。

### 13.3 端到端测试

- [ ] 访客浏览和播放完整流程。
- [ ] 管理员登录和退出。
- [ ] 新建 Unit 并上传视频。
- [ ] 上传自定义封面并在公开页面显示。
- [ ] 下架视频后公开页面不可见。
- [ ] 排序调整后公开页面顺序正确。
- [ ] 未登录用户不能执行管理操作。

### 13.4 设备与浏览器

- [ ] iPhone/iPad Safari。
- [ ] Android Chrome。
- [ ] 桌面 Chrome、Safari 和 Edge。
- [ ] 横屏、竖屏、触屏和全屏播放。

## 14. 上线检查清单

- [ ] 所有 migration 已执行。
- [ ] 至少创建一个管理员，默认密码已更换。
- [ ] 生产环境 Cookie 开启 `Secure`。
- [ ] HTTPS 证书有效。
- [ ] 上传和媒体目录权限正确。
- [ ] 24 个现有视频和封面迁移完成。
- [ ] 公开页面不展示草稿和下架内容。
- [ ] Nginx Range 请求验证通过。
- [ ] 数据库和媒体备份任务已启用。
- [ ] 健康检查和日志可用。
- [ ] 删除回收任务处于启用状态。

## 15. Definition of Done

项目达到以下条件才视为完成：

- [ ] `spec.md` 中的 MVP 功能和验收标准全部满足。
- [ ] 未登录访客只能查看和播放已发布内容。
- [ ] 管理员可以在网页中完成 Unit、视频和封面的全流程管理。
- [ ] 上传、认证、排序、发布、删除和恢复均有自动测试。
- [ ] 当前 24 个视频和 24 个封面成功迁移且可播放。
- [ ] Docker Compose 可以在空白服务器上启动系统。
- [ ] HTTPS、Nginx、持久化卷、备份和恢复文档完整。
- [ ] 不存在已知高危安全问题和数据丢失路径。
